name: CreateRelease
on:
  pull_request:
    types: [closed]

jobs:
  create-release:
    if: github.event.pull_request.merged == true
    uses: Tools4everBV/.github/.github/workflows/createReleaseV7.yaml@main
