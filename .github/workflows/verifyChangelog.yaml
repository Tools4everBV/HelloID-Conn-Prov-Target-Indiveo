name: VerifyChangelog
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  verifychangelog:
    uses: Tools4everBV/.github/.github/workflows/verifyChangelogV3.yaml@main
